# Sample environment configuration for NAS Gallery
# Copy to .env and adjust values (never commit real secrets in .env)

# Absolute path or mounted path to the NAS photo root
IMAGE_ROOT=/mnt/nas/photos

# Basic Auth credentials (initial simple protection inside LAN)
BASIC_USER=viewer
# For now we allow a plain: prefix. Later replace with bcrypt hash (e.g. bcrypt:$2b$10$...)
BASIC_PASS_HASH=plain:change_me

# Thumbnail width (public runtime config)
THUMBNAIL_WIDTH=320

# Scan concurrency (defaults to CPU cores if unset)
SCAN_CONCURRENCY=8

# Optional: port override for dev server
# NUXT_PORT=3000

# Notes:
# - Do NOT commit the generated .env file.
# - Use `SCAN_CONCURRENCY` cautiously on low-memory NAS.
# - When moving to bcrypt, generate a hash (node -e "console.log(require('bcrypt').hashSync('yourpass',10))") and set BASIC_PASS_HASH=bcrypt:<hash>
